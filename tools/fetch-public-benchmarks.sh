#!/usr/bin/env bash
set -euo pipefail

cat <<'MSG'
fetch-public-benchmarks.sh is a G0 placeholder.

Future behavior:
  - fetch approved public LP benchmark instances;
  - verify checksums;
  - write metadata into instances/public/manifest.json.
MSG
