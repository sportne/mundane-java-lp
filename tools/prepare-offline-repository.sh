#!/usr/bin/env bash
set -euo pipefail

cat <<'MSG'
prepare-offline-repository.sh is a G0 placeholder.

Future behavior:
  - resolve Gradle plugin and dependency artifacts;
  - copy them into a checksum-manifested Maven repository;
  - print mlp.offlineRepo configuration instructions.
MSG
