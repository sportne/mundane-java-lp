#!/usr/bin/env bash
set -euo pipefail

cat <<'MSG'
normalize-benchmark-results.sh is a G0 placeholder.

Future behavior:
  - read raw harness JSON;
  - produce normalized summary CSV/Markdown;
  - preserve failures and unavailable solvers.
MSG
