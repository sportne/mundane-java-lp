# lp-solver-spi

Solver service-provider interface module. It defines the solver input envelope,
normalized solver result evidence, options, solver IDs, and work-directory
contract used by CLI adapters, Java-library adapters, and in-project solvers
such as `lp-solver-simple`.

See the repository root `README.md` and `docs/architecture/module-boundaries.md`.
