package io.github.mundanej.mlp.model;

/** Direction for the linear objective. */
public enum ObjectiveSense {
    /** Minimize the objective. */
    MINIMIZE,
    /** Maximize the objective. */
    MAXIMIZE
}
