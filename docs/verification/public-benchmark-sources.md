# Public benchmark sources

G0 does not vendor public benchmark instances.

Future public benchmark ingestion must record:

- upstream source URL;
- license or redistribution terms;
- download date;
- checksum;
- local normalization steps;
- instance-level metadata.

Use `instances/public/` for local copies. That directory is git-ignored.
